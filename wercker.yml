box: wercker/rvm@8.0.0
build:
  steps:
    - bundle-install:
        cwd: site/contents
    - script:
        name: jekyll build
        cwd: site/contents
        code: bundle exec jekyll build --trace --destination "$WERCKER_OUTPUT_DIR"

deploy:
  steps:
    - add-ssh-key:
        keyname: DEPLOY_PRODUCTION
    - add-to-known_hosts:
        hostname: mountainwerks.org
